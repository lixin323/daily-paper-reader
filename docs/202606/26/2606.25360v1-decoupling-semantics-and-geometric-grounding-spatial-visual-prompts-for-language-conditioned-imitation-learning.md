---
title: "Decoupling Semantics and Geometric Grounding: Spatial Visual Prompts for Language-Conditioned Imitation Learning"
title_zh: 解耦语义与几何基础：面向语言条件模仿学习的空间视觉提示
authors: "Yanzhe Tang, Xinyu Shao, Yuxuan Hu, Siyu Chen, Bowen Yang, Yajun Gao, Tongtong Cao, Xiu Li, Long Zeng"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25360v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: 解耦VLA模型中的语义与几何接地用于操作
tldr: "端到端VLA模型将语义推理与空间控制耦合，导致数据有限的模仿学习中目标歧义难以消解。本文提出SVP-IL解耦架构，利用视觉语言基础模型从指令生成零样本几何掩码作为空间视觉提示，通过轻量级特征融合注入连续动作生成器。在50-100个演示训练下，平均成功率从24.0%提升至39.5%，标准基准达67.8%。该工作为语言条件模仿学习提供了高效解耦范式，显著提升数据效率与鲁棒性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25360v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 513, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25360v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1817, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25360v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 813, \"height\": 833, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25360v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 899, \"height\": 872, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25360v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 908, \"height\": 822, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25360v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 808, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25360v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 907, \"height\": 868, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25360v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 907, \"height\": 294, \"label\": \"Table\"}]"
motivation: 克服端到端VLA模型在数据有限时语义推理与空间控制的耦合对齐瓶颈，实现精确目标解歧义。
method: 利用视觉语言基础模型从指令解析零样本几何掩码作为空间视觉提示，通过轻量级特征融合注入动作生成器。
result: "在50-100个演示下，平均成功率从24.0%提升至39.5%，标准基准达67.8%，真实实验验证鲁棒性。"
conclusion: 解耦语义与空间控制可有效解决对齐问题，实现数据高效的模仿学习。
---

## 摘要
尽管端到端的视觉-语言-动作（VLA）模型在机器人操作中展现出潜力，但其单一范式本质上耦合了语义推理与空间控制。这造成了严重的对齐瓶颈，限制了在数据受限的模仿学习中的精确目标消歧。为解决这一问题，我们提出SVP-IL，一种解耦架构，显式地将空间视觉基础从动作生成循环中提取出来。通过利用视觉语言基础模型，我们将指令解析为零样本几何掩码，将语言转化为显式的空间视觉提示（SVP）。这些先验知识通过轻量级的直接特征级融合机制注入到连续动作生成器中。这种集成提供了显式且无损坏的空间梯度引导，同时确保在低数据情况下高度稳定的优化。大量实验表明，SVP-IL显著优于最先进的VLA和纯视觉运动基线。在仅用50到100个演示训练的情况下，SVP-IL将高度模糊的语言条件任务的平均成功率从24.0%提高到39.5%，在标准基准上达到67.8%。真实机器人实验进一步验证了其在非结构化物理环境中的鲁棒性和数据效率。

## Abstract
While end-to-end Vision-Language-Action (VLA) models show promise in robotic manipulation, their monolithic paradigm inherently couples semantic reasoning and spatial control. This creates a severe alignment bottleneck, limiting precise target disambiguation in data-constrained imitation learning. To overcome this, we propose SVP-IL, a decoupled architecture that explicitly extracts spatial visual grounding from the action generation loop. By leveraging vision-language foundation models, we parse instructions into zero-shot geometric masks, translating language into explicit Spatial Visual Prompts (SVP). These priors are injected into a continuous action generator via a lightweight direct feature-level fusion mechanism. This integration provides explicit and uncorrupted spatial gradient guidance while ensuring highly stable optimization under low-data regimes. Extensive experiments demonstrate that SVP-IL significantly outperforms state-of-the-art VLAs and pure visuomotor baselines. Trained on as few as 50 to 100 demonstrations, SVP-IL improves average success rates on highly ambiguous language-conditioned tasks from 24.0% to 39.5%, achieving 67.8% on standard benchmarks. Real-world robotic experiments further validate its robustness and data efficiency in unstructured physical environments.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：数据驱动的模仿学习在机器人操作中取得了进展，端到端视觉-语言-动作（VLA）模型通过直接映射视觉观测和语言指令到动作展示了通用策略的潜力。然而，这类模型将语义理解、空间定位和连续动作生成完全耦合在单一网络中。
- **核心问题**：这种耦合导致了严重的**语义-空间对齐瓶颈**。将离散语言令牌转换为精确空间控制在数据受限的模仿学习场景中极其低效，尤其在多对象、杂乱环境下，模型难以隐式学习跨模态对齐，容易产生空间不精确和接近失败的操作。
- **整体意义**：本文提出解耦感知-控制架构，通过显式分离语义推理与几何定位，为模仿学习提供关键的空间归纳偏置，从而在极低数据量下显著提升语言条件任务的鲁棒性和成功率。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- **解耦架构**：将语义目标消歧从动作生成循环中剥离，利用现成的视觉-语言基础模型将语言指令直接转换为显式的空间视觉提示（SVP）——二进制几何掩码。这使得连续控制策略只需专注于空间执行，无需从零学习语言到像素的映射。
- **轻量级特征级融合**：为了避免图像级融合（如叠加掩码或通道拼接）破坏预训练视觉骨干的纹理特征，提出在深度中间潜空间进行直接元素级加法融合，提供无损坏的空间梯度引导。

### 关键技术细节
1. **空间视觉提示生成**（两阶段）：
   - 阶段1：使用大语言模型（LLM）解析原始语言指令 `l`，提取目标对象名称 `c = LLM(l)`。
   - 阶段2：利用 SAM 3 模型，以 `c` 为文本提示从当前 RGB 图像 `I_t` 中零样本生成二进制掩码 `M_t = SAM3(I_t, c)`。
2. **提示特征提取与融合**：
   - 设计一个轻量的侧流卷积神经网络（Side-CNN）`Φ_prompt`，将稀疏掩码 `M_t` 编码为与基础视觉编码器某一中间层 `i` 空间尺寸匹配的密集特征图 `F_prompt^{(i)}`。
   - 采用**直接残差特征融合**：`F^{(i)} = Φ_rgb^{(i)}(I_t) + F_prompt^{(i)}`。元素级加法作为非参数化的结构偏置，强制网络立即关注空间先验，避免额外可学习门控机制在低数据下的过拟合。
3. **动作生成**：增强后的视觉特征与本体状态、文本嵌入拼接，作为扩散策略（Diffusion Policy）1D U-Net的全局条件，预测动作块，实现平滑执行。
4. **系统集成**：使用同步推理流水线，LLM仅查询一次提取目标名，随后每步调用SAM3生成掩码。为平衡效率，图像和掩码下采样后送入策略，基础策略运行在10Hz，通过动作插值实现300Hz底层控制。

## 3. 实验设计：数据集/场景、benchmark、对比方法

### 数据集/场景
- **模拟环境**：RoboTwin 2.0 高保真双机械臂操作基准。
  - 两种视觉域：**Clean**（极简背景）和 **Randomized**（杂乱背景、动态光照、密集干扰物）。
  - 9个标准任务（如 Adjust Bottle, Lift Pot, Click Bell 等）。
  - 6个自定义**语言条件消歧任务**（如 "Place the [specific object] in the basket"），目标对象随指令变化，用于严格评估语义识别能力。
- **真实世界**：Aloha-AgileX 双机械臂平台，包含三个密集杂乱任务：
  - Table Setting（堆叠同形状不同颜色的碗到盘子）
  - Desktop Cleaning（区分相似颜色但不同语义和形状的对象，如黄色玉米 vs 黄色香蕉）
  - Bimanual Sorting（根据语义选择正确的手臂抓取左右侧目标）

### Benchmark
- 模拟任务成功率（%）对比。
- 真实任务成功率（%）对比。

### 对比方法
- **纯视觉运动基线**：Action Chunking with Transformers (ACT)、Diffusion Policy (DP)
- **VLA基线**：π0（π-zero）—— 使用官方默认配置全参数微调。
- 消融对比：
  - Pixel-Overlay（掩码叠加到RGB）
  - Early-Concat（将掩码作为第4通道拼接）
  - 不同训练环境（Clean vs Random）的对比

## 4. 资源与算力

论文**未明确说明**所使用的GPU型号、数量、训练时长等具体算力信息。仅在方法部分提及使用现成的SAM 3和LLM（可能为云端服务或本地部署），策略训练在50~100个演示数据上进行，但未提供硬件细节。文中提到在真实机器人部署中，策略在10Hz运行，但未给出训练设备规格。

## 5. 实验数量与充分性

- **模拟实验**：
  - 9个标准任务 × 多种方法（DP、ACT、π0、SVP-IL）各训练50个演示。
  - 6个自定义任务 × 多种方法各训练100个演示。
  - 环境鲁棒性对比：Clean vs Random 训练/评估，覆盖 DP 和 SVP-IL。
  - 消融实验：对比 Pixel-Overlay、Early-Concat、SVP-IL 在基本和自定义任务上的表现。
- **真实实验**：三大任务 × 三个方法（DP、π0、SVP-IL）各进行20次独立试验。
- **充分性评价**：实验设计较为全面，覆盖了标准基准、困难消歧任务、分布偏移鲁棒性、消融分析以及真实部署，客观性和公平性较好（统一训练配置、多轮评估）。但缺少统计显著性检验（如置信区间），且部分任务（如 Hanging Mug）成功率极低，可能受限于任务难度和数据量。

## 6. 主要结论与发现

1. **性能显著提升**：SVP-IL 在 RoboTwin 2.0 标准任务上平均成功率 67.8%，远超 π0（53.7%）、ACT（45.2%）、DP（42.3%）。在自定义语言消歧任务上，从 π0 的 24.0% 提升至 39.5%，几乎翻倍。
2. **对视觉分布偏移的鲁棒性优于域随机化**：即使使用域随机化训练，DP 在低数据下因过拟合而崩溃（14.9%），而 SVP-IL 仍保持 24.0%。更关键的是，SVP-IL 用 50 个 Clean 演示即可达到 67.8%，远超 DP 在任何条件下的表现。
3. **特征级融合最优**：消融实验证实，元素级加法融合（39.5%）显著优于像素覆盖（20.0%）和早期通道拼接（22.2%），说明保护预训练纹理特征的重要性。
4. **真实环境可行性**：在物理 Aloha-AgileX 上，SVP-IL 平均成功率 60.0%，大幅领先 DP（28.3%）和 π0（31.7%），尤其是在需要颜色区分和手臂路由的任务中表现突出。

## 7. 优点

- **方法论创新**：提出“解耦语义与空间”的范式，将重语义理解外包给基础模型，使小策略专注于执行，有效解决对齐瓶颈。
- **轻量化融合设计**：直接元素级加法 + 侧流 CNN，既避免了输入级域偏移，又提供强空间梯度引导，在极低数据下保持稳定优化。
- **通用性**：框架可即插即用于现有模仿学习策略（如 Diffusion Policy），无需大量架构修改。
- **实验覆盖全面**：既包含标准基准又设计了专门的语言消歧任务，同时验证了域随机化失效、消融分析、真实机器人部署，说服力较强。
- **数据效率突出**：仅用 50~100 个演示即实现显著性能提升，对实际应用有重要价值。

## 8. 不足与局限

1. **上游模型的依赖性**：性能受限于 SAM 3 的零样本分割能力，在透明、强反光、严重遮挡物体上可能产生噪声掩码，导致策略精度下降。
2. **指令形式限制**：目前需要显式的、以对象为中心的指令（如“拿起红色苹果”），无法处理隐式或抽象意图（如“收拾桌子”），未来需扩展对抽象意图的推理。
3. **计算开销**：SAM 3 作为实时分割器带来延迟，虽然通过动作块和插值缓解，但在高速动态环境中可能影响反应性。论文指出其优势在静态杂乱场景中突出，但动态场景受限。
4. **实验覆盖的局限性**：
   - 仅在一个模拟基准（RoboTwin 2.0）和一种真实平台（Aloha-AgileX）上验证，缺乏跨平台、跨任务泛化性测试。
   - 未报告统计波动（标准差或置信区间），可能高估单次评估的稳定性。
   - 部分任务（如 Hanging Mug）成功率极低（24%），暗示方法在精细操作上仍有提升空间。
   - 缺乏对训练数据多样性（如不同光照、材质组合）的系统探索。
5. **算力信息缺失**：未提供训练所需的GPU型号、数量、时长，降低了可复现性参考。
6. **安全与鲁棒性**：未讨论当上游模型输出错误掩码时的故障处理或降级策略。

（完）
