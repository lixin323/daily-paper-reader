---
title: Task Robustness via Re-Labelling Vision-Action Robot Data
title_zh: 通过重新标记视觉-动作机器人数据实现任务鲁棒性
authors: "Artur Kuramshin, Özgür Aslan, Cyrus Neary, Glen Berseth"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10918v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: 使用视觉语言模型重新标注视觉-动作数据以增强机器人操作
tldr: 机器人模仿学习策略难以泛化到新指令，主要受限于数据集的语言和动作多样性不足。本文提出TREAD框架，利用视觉语言模型（VLM）将原始指令分解为多个语义子任务，并对演示视频进行分割，生成包含物体属性的多样化指令，从而将长程演示转化为语言-动作对。进一步通过文本目标的语言多样化增强鲁棒性。在LIBERO任务上，该数据增强方法使得训练策略在未见任务上显著提升性能。TREAD无需额外数据收集，通过轨迹分解和语言多样性增强了规划泛化和语言条件策略泛化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10918v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1562, \"height\": 763, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10918v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1279, \"height\": 861, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10918v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1821, \"height\": 250, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10918v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1516, \"height\": 551, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10918v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1358, \"height\": 257, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10918v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1373, \"height\": 424, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10918v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1449, \"height\": 553, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10918v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 892, \"height\": 346, \"label\": \"Table\"}]"
motivation: 现有机器人数据集语言和动作多样性不足，导致策略难以泛化到新指令，亟需低成本数据增强方法。
method: TREAD利用预训练VLM，通过子任务生成、视频分割和多样化指令重标注三步，将原始演示分解为语言-动作对并增强语言多样性。
result: 在LIBERO基准上，使用TREAD增强数据训练的策略在未见任务上表现显著优于基线，提升了规划泛化和语言条件泛化。
conclusion: TREAD是一种可扩展的数据增强框架，无需额外数据收集即可提升机器人策略的指令遵循能力和任务鲁棒性。
---

## 摘要
近年来，机器人学习中的模型规模化趋势产生了令人印象深刻的策略，这些策略能够执行各种操作任务并泛化到新场景。然而，这些策略在遵循指令方面仍然存在困难，这很可能是因为现有机器人数据集中语言和动作序列的多样性有限。本文介绍了TREAD（通过重新标记视觉-动作机器人数据实现任务鲁棒性），这是一个可扩展的框架，利用大型视觉语言模型（VLM）来增强现有机器人数据集，无需额外收集数据，并利用了这些模型中嵌入的可迁移知识。我们的方法通过三个阶段利用预训练的VLM：从原始指令标签和初始场景生成语义子任务，基于这些子任务对演示视频进行分割，以及生成结合物体属性的多样化指令，从而有效地将较长的演示分解为具体的语言-动作对。我们进一步通过增加文本目标的语言多样性版本来增强鲁棒性。在LIBERO上的评估表明，在我们的增强数据集上训练的策略在全新、未见过的任务和目标上表现出更优的性能。我们的结果表明，TREAD通过轨迹分解增强了规划泛化，并通过增加语言多样性增强了语言条件策略的泛化。

## Abstract
The recent trend in scaling models for robot learning has resulted in impressive policies that can perform various manipulation tasks and generalize to novel scenarios. However, these policies continue to struggle with following instructions, likely due to the limited linguistic and action sequence diversity in existing robotics datasets. This paper introduces Task Robustness via Re-Labelling Vision-Action Robot Data (TREAD), a scalable framework that leverages large Vision-Language Models (VLMs) to augment existing robotics datasets without additional data collection, harnessing the transferable knowledge embedded in these models. Our approach leverages a pretrained VLM through three stages: generating semantic sub-tasks from original instruction labels and initial scenes, segmenting demonstration videos conditioned on these sub-tasks, and producing diverse instructions that incorporate object properties, effectively decomposing longer demonstrations into grounded language-action pairs. We further enhance robustness by augmenting the data with linguistically diverse versions of the text goals. Evaluations on LIBERO demonstrate that policies trained on our augmented datasets exhibit improved performance on novel, unseen tasks and goals. Our results show that TREAD enhances both planning generalization through trajectory decomposition and language-conditioned policy generalization through increased linguistic diversity.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：当前机器人学习中的视觉-语言-动作（VLA）策略在**遵循自然语言指令**方面表现不佳，尤其是在面对未见过的任务或语言变体时。这主要归因于现有机器人数据集中**语言多样性和动作序列多样性不足**——数据集往往包含较长的完整轨迹，但每个轨迹仅对应一个单一指令，导致语言-动作对的稀疏性，且缺乏对子目标层次的细粒度标注。
- **研究动机**：尽管机器人社区通过大规模协作扩大了数据集规模，但数据收集成本高、耗时长，难以手动为每条轨迹标注丰富的子任务和多样化指令。因此，需要一种**可扩展的、无需额外收集数据**的方法来增强现有数据集的语言-动作多样性。
- **整体含义**：TREAD 旨在通过**利用预训练视觉语言模型（VLM）的零样本能力**，自动将已有的长程演示分解为多个语义子任务，并为每个子任务生成多样化、具身化的语言指令，从而提升策略的规划泛化能力和指令遵循鲁棒性。

#### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程
- **核心思想**：使用一个大型预训练 VLM（此处为 Gemini Pro 2.5），通过**迭代式三阶段流程**对原始数据集中的每条轨迹进行分解和增强：
    1. **子任务分解（Subtask Decomposition）**：给定原始任务指令 ℓ 和轨迹的第一帧图像 o₁，VLM 生成一个有序的**语义子任务列表**（如“抓取白色杯子”→“提起杯子”→“将杯子放在盘子上”）。子任务的数量和内容会根据场景中物体的状态（如抽屉是否已打开）动态调整。
    2. **动作分割（Motion Segmentation）**：将整个轨迹的视频输入 VLM，并提示其识别每个子任务对应的**起始和结束时间码**，从而将原始轨迹切分为多个子轨迹（sub-trajectories），每个子轨迹附带对应的子任务标签。
    3. **语言多样化（Language Diversity）**：对每个子轨迹，再次利用 VLM，以子任务标签和初始帧为上下文，生成 **k 个语义等价但语言表达不同的指令**（如加入物体颜色、空间关系等），从而增强文本多样性。

- **关键技术细节**：
  - 使用**链式提示（chaining）**：先产生子任务列表，再基于该列表进行视频分割；论文指出“一次性要求 VLM 分割视频”效果差。
  - **零样本方式**：VLM 无需针对机器人任务进行微调，直接利用其互联网预训练知识。
  - 增强后的数据集与原始数据集混合训练，采用 **Re-Mix 策略**确定最佳混合比例（消融了 1:2、1:1.5、Re-Mix 等）。
  - 训练目标为标准的**行为克隆（Behavior Cloning）**：最小化预测动作分布与演示动作之间的负对数似然。

- **算法流程（伪代码）**：
  - 输入：原始数据集 D，VLM G
  - 步骤1：对 D 中每条轨迹，调用 `DECOMPOSE(D, G)` 得到分解后的子轨迹数据集 ˜D_A
  - 步骤2：对 ˜D_A，调用 `DIVERSIFY(˜D_A, G)` 得到具有多样化指令的数据集 ˜D_H
  - 步骤3：分别训练策略 π_A（混合 D 和 ˜D_A）和 π_H（混合 D 和 ˜D_H）

#### 3. 实验设计：数据集、Benchmark、对比方法
- **数据集**：**LIBERO-100**（完整 100 个任务，每个任务 50 条演示）。受限于资源，实际标注了 5 条演示/任务，共 570 条轨迹（排除了 STUDY_SCENE 场景）。同时使用 **LIBERO-10** 作为分布内测试集。
- **Benchmark**：自定义两个测试套件：
  - **运动泛化（Motion Generalization, MG）**：将现有指令与以往未配对的新场景组合，共 7 个任务。
  - **语言泛化（Language Generalization, LG）**：对 14 个已有任务修改指令（调整语序、删除冗余修饰词），共 14 个任务。
- **对比方法**：
  - **原始微调（Original Fine-tuned）**：直接使用原始数据微调 Octo 或 π0-FAST。
  - **TREAD w/o diverse labels**：混合原始数据 + 分解后子轨迹（未做语言多样化）。
  - **TREAD**：混合原始数据 + 具有多样化指令的子轨迹。
- **策略模型**：Octo-Small 1.5（扩散策略，预训练于 Open-X 数据集）和 π0-FAST（基于 PaliGemma 的 VLM + FAST 动作离散化，预训练于 π0 数据混合）。

#### 4. 资源与算力
- 文中未明确说明具体使用的 GPU 型号、数量及训练总时长。仅提及：
  - 计算资源来自“Digital Research Alliance of Canada, Mila IDT, NVidia”。
  - 训练细节：Octo 训练 50,000 步，batch size 256，峰值学习率 3e-4；π0-FAST 训练 30,000 步，batch size 32，峰值学习率 2.5e-5。
  - VLM 使用 Google 的 Gemini Pro 2.5（闭源 API），未提供推理成本。
- **不足**：由于未披露具体硬件，难以复现或评估训练成本。

#### 5. 实验数量与充分性
- **实验数量**：主要包括：
  - 两个策略模型（Octo、π0-FAST）在三个数据变体上的对比。
  - 两组测试套件（MG 和 LG），每组包含多个任务，各进行 30 次 rollout，报告平均成功率及标准误差。
  - 数据集混合比例消融（1:2、1:1.5、Re-Mix），并在 Best 比例下报告最终结果。
  - 另在 LIBERO-10 上测试了分布内性能。
- **充分性评估**：
  - **充分**：覆盖了规划泛化和语言泛化两个关键维度，分别评估了子任务分解和语言多样化的独立贡献。
  - **客观公平**：消融设计合理，基线包括无增强和部分增强，使用了相同的超参数和训练框架。
  - **局限性**：自定义测试集规模较小（MG 7 任务，LG 14 任务），且未在更大规模数据集（如 DROID、BridgeV2）上验证；部分实验结果（如双目标任务的完全成功率较低）暗示长程任务仍存在挑战。

#### 6. 论文的主要结论与发现
1. **轨迹分解有效提升规划泛化**：在运动泛化任务中，TREAD（含分解）相较于原始微调，在单目标任务上提升了 6-15%，在多目标任务的第一目标完成率上提升了 9-30%（Octo 提升更显著）。
2. **语言多样化改善指令跟随**：在语言泛化任务中，TREAD 相比原始微调，单目标任务成功率提升 9-20%，表明多样化的文本目标增强了策略对指令措辞变化的鲁棒性。
3. **不损害分布内性能**：在 LIBERO-10 上，TREAD 与原始微调的双目标完全成功率持平，说明增强不会破坏已有能力。
4. **Octo 受益更大**：Octo 缺乏预训练的视觉-语言理解能力，因此从子任务分解和语言多样化中获得更大的相对提升。
5. **方法简单高效**：仅需一个 VLM 的零样本推理，无需手动标注或额外数据收集。

#### 7. 优点
- **无需额外数据收集**：完全基于已有演示进行自动分割和重标注，显著降低数据增强成本。
- **方法通用**：可应用于任何视觉-动作数据集，且与策略架构无关（实验验证了两种不同结构）。
- **零样本利用 VLM**：避免了对 VLM 进行领域内微调，利用了其互联网预训练的泛化知识。
- **双管齐下**：同时解决“动作序列多样性”和“语言多样性”两个瓶颈，并设计了消融分析验证各自贡献。
- **与现有基准兼容**：增强后的数据可直接用于训练现有 VLA 模型，无需修改训练流程。

#### 8. 不足与局限
- **依赖闭源 VLM**：使用 Gemini Pro 2.5（闭源 API），导致完全复现困难（即使提供提示词，不同 API 版本可能输出不一致）。作者也建议未来使用开源模型（如 Qwen2.5-VL、VideoLLaMA）以提升可复现性。
- **标注质量上限**：VLM 可能在长视频分割中产生错误的时间边界或子任务标签，但论文未进行严格的标注质量评估（如人工评估准确率）。
- **实验规模有限**：仅在 LIBERO 仿真环境上评估，且只标注了 570 条轨迹；未在真实机器人或更大规模数据集（如 Open X-embodiment）上测试，泛化能力存疑。
- **语言多样性可能引入噪声**：VLM 生成的多样化指令可能包含语义偏差或难以执行的描述（如过于模糊），但论文未分析这种情况对策略训练的影响。
- **长程任务提升有限**：对于双目标任务的完全成功率（两个子目标均完成），TREAD 提升不明显甚至部分下降（如 π0-FAST 在 MG 的“2 of 2”成功率为 0%），表明长程任务仍存在执行误差累积问题。
- **未与同类方法直接比较**：虽然提到了 DIAL、NILS、SPRINT 等，但实验未将这些方法作为对比基线（仅在 Related Work 中讨论），降低了方法间公平比较的强度。

（完）
